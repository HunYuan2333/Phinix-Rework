# 当前遗留问题与稳定性汇总

> 基于以下两份历史文档合并并按当前代码状态重新核对：
> - `已知未解决问题.md`
> - `性能并发与稳定性审计报告.md`
>
> 最后核对日期：2026-06-01

---

## 1. 总体结论

当前代码并不是“历史问题全部未修”，而是处于下面这个状态：

- 设计哲学合规整改已完成一大轮，部分旧文档条目已经过时
- 先前最危险的一批客户端线程越界点已完成静态修复
- 当前剩余的主要问题已经收缩到少量结构债和黑盒验证缺口

---

## 2. 已确认仍未解决的问题

### 2.1 仍存在的性能/稳定性中低优先级问题

| 项目 | 严重度 | 当前状态 |
|------|--------|----------|
| [TextWidget.cs:54](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Client\Source\GUI\Basic Widgets\TextWidget.cs:54>) 文本宽度计算仍在热路径 | LOW | 已降分配，但仍建议黑盒观察 UI 绘制开销 |
| [TextFieldWidget.cs:31](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Client\Source\GUI\Basic Widgets\TextFieldWidget.cs:31>) 默认正则仍是通用校验路径 | LOW | 已静态化 |

---

## 3. 已确认修复完成的历史问题

### 3.1 `PlayNoiseOnMessageReceived` 仍在 `IClientSettingsContext`

**历史文档结论：** 未修  
**当前代码状态：** 已修

**证据**

- 现在的 [IClientExtensionAbstractions.cs](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Client\ClientExtensionAbstractions\Framework\IClientExtensionAbstractions.cs:78>) 中，`IClientSettingsContext` 已不再包含该属性
- 兼容入口保留在 [Settings.cs](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Client\Source\Settings.cs:37>)，并映射到 `chat.playNoiseOnMessageReceived`
- Chat 自己的设置 UI 位于 [ChatSettingsPanelProvider.cs](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Extensions\Chat\Client\ChatSettingsPanelProvider.cs:22>)

---

### 3.2 `Config.cs` 硬编码 Chat/Trade XML 命名空间迁移

**历史文档结论：** 未修  
**当前代码状态：** 已修

**证据**

- [Server\Config.cs](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Server\Config.cs:260>) 现在通过 `ILegacyExtensionConfigMigrator` 做扩展自迁移
- [ChatServerConfig.cs](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Extensions\Chat\Server\ChatServerConfig.cs:30>) / [TradeServerConfig.cs](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Extensions\Trade\Server\TradeServerConfig.cs:25>) 各自实现迁移
- `Server\Config.cs` 中已无 `Phinix.ChatExtension.Server` / `Phinix.TradeExtension.Server` 的硬编码迁移字符串

---

### 3.3 设计哲学合规整改的一批宿主泄漏问题

**当前代码状态：** 已修

- `Client.CanUseFrameworkChat` 已移除
- 登录时不再注入 `acceptingTrades`
- `WriteSettings()` 不再由 host 主动同步 Trade 业务状态
- `ServerUserManager` 已通过 `IServerUserManager` 抽象暴露
- Chat client 对 Trade contracts 的编译期引用已移除
- Trade legacy repository/completion 责任已从主 service 分离

---

### 3.4 线程安全与稳定性第一轮热点

**当前代码状态：** 已修，待黑盒验证

- `TradeWindow` 的 `OnTradeUpdated` / `OnTradeFinished` 已切回主线程执行
- `thing.Destroy()` / `GenSpawn.Spawn()` / `refreshAvailableItems()` 不再直接从交易更新回调线程触发
- `Client.cs` 中认证失败、登录失败、连接失败、凭证请求弹窗已统一经主线程打开
- `ClientAuthenticator` 的 `lock (credentialStore)` 已改为 `lock (credentialStoreLock)`
- `TypeUrl` 正则已静态化
- `TradeList` 打开交易窗口已改经 `hostContext.OpenTradeWindow(...)`
- `StackedThings.Count` 已移除 `Sum(...)`
- `ChatMessageList` 已移除时间戳/名字测量处的 `GUIContent` 分配
- `SettingsWindow` 已用 `ThreadPool.QueueUserWorkItem(...)` 取代 `new Thread(...)`

---

### 3.5 Common 程序集拆分

**历史文档结论：** 未修  
**当前代码状态：** 基本已修

**证据**

- [Authentication.csproj](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Common\Authentication\Authentication.csproj>) 已排除 `ClientAuthenticator.cs`
- [Connections.csproj](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Common\Connections\Connections.csproj>) 已排除 `NetClient.cs` / `NetServer.cs`
- [UserManagement.csproj](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Common\UserManagement\UserManagement.csproj>) 已排除 `ClientUserManager.cs`
- client/server 分别通过单独项目重新链接这些实现文件

---

### 3.6 客户端 legacy 设置迁移

**历史文档结论：** 未修  
**当前代码状态：** 已修

**证据**

- [Client.cs](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Client\Source\Client.cs:141>) 现在在扩展加载后统一调用 `MigrateLegacySettings(...)`
- [IClientExtensionAbstractions.cs](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Client\ClientExtensionAbstractions\Framework\IClientExtensionAbstractions.cs:132>) 新增 `IClientLegacySettingsMigrator`
- [ChatSettingsPanelProvider.cs](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Extensions\Chat\Client\ChatSettingsPanelProvider.cs:53>) / [TradeSettingsPanelProvider.cs](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Extensions\Trade\Client\TradeSettingsPanelProvider.cs:39>) 各自处理本插件旧 key
- [Settings.cs](<C:\Users\zydyo\Desktop\Phinix\Phinix-Rework\Client\Source\Settings.cs:228>) 只负责收集 legacy 值并分发给迁移器

---

## 4. 已修但仍需黑盒 / 端到端验证

| 项目 | 说明 |
|------|------|
| LegacyAdapter E2E | 需要连接原版 Phinix 服务器走完整聊天/交易链路 |
| TradeWindow 回归 | 需要做双端交易确认、取消、完成、失败回滚回归，确认“功能语义没变，只是线程收口” |
| Prepatcher / ProbeDirectories 回归 | 需要在真实部署环境验证 |
| Chat 交互回归 | 需要确认 blocked users、消息绘制、右键菜单行为未受缓存改动影响 |

---

## 5. 建议优先级

1. 黑盒验证 Trade / Chat 主路径  
2. 再决定是否继续做 `Settings` 旧迁移逻辑插件化  
3. 最后处理真正剩下的结构债或更细的 UI 性能优化

---

## 6. 说明

- 本文档是静态代码核对结果，不等于黑盒通过结果
- 编译通过不代表这些线程安全问题已消失
- 历史文档中“未修复”的部分结论已经过时，因此不再保留原文
